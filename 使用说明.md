# DESK HARNESS · 使用说明

## 这是什么

`deepseekharness.exe` 是 **DESK HARNESS**——DeepSeek Harness 的 Windows 桌面版（Electron 封装）。
窗口内加载的就是网页版本身（`http://127.0.0.1:3080/`），因此**网页版的所有功能和界面完整保留**。
桌面版额外提供：

### 启动体验
- **科技感启动页**：打字机状态；**启动检查**逐项显示运行环境（Node.js 是否安装、版本是否 ≥ v22.19.0）与本地服务状态（检测/启动/就绪）
- **首次使用引导**：第一次启动展示如何配置 API Key（platform.deepseek.com → API Keys），一键打开 API Key 页面，点「开始使用」后不再出现
- **启动倒计时 3 秒**：默认倒计时后自动进入；可点「立即进入」，勾选「以后不再等待」永久跳过（可在控制面板重新开启）
- **自定义启动界面**：控制面板「🚀 启动界面」可换启动页背景图、自定义欢迎语、开关倒计时、重新查看首次引导
- 后端无服务时自动拉起（`dsh web`），有服务直接复用

### 标题栏与状态
- 自绘标题栏（渐变 + 毛玻璃 + 霓虹分界线）：☰ 菜单、会话状态、Token 胶囊、上下文占用条
- **💰 余额胶囊**：标题栏实时显示 DeepSeek 账户余额，**每 5 分钟自动查询一次**（读取 `~/.dsh/.credentials.yaml` 中的 API Key）；点击打开官方用量页（platform.deepseek.com/usage）；问号 `?` 悬停提示说明。**简洁模式（无标题栏）下自动切换为右上角浮动余额胶囊**，功能一致
- Token 用量胶囊：明确标注【当前会话】统计口径，展示 输入/输出/缓存命中率，每个指标带 ? 悬停解释；悬停胶囊查看明细卡（当前会话拆分 + 全局累计，含上下文占用、轮次/步数/耗时）
- 任务进度条（底部霓虹条 + Windows 任务栏进度联动，基于当前会话 todos/任务状态）

### 面板与工具
- **终端停靠面板**（标题栏 `>_` 或 **Ctrl+`**）：从窗口右侧平滑滑出，**不压缩、不改变中间对话区尺寸**；工作目录默认工作区根目录，`cd` 可切换；中文输出正常显示（UTF-8/GBK 双解码）；宽度可拖拽调整并记忆
- **文档阅读**：拖入文档即可阅读（Markdown/文本/代码），支持多标签、编辑与保存
- **插件商店 dsh-plugin**：标题栏「🧩 dsh-plugin」按钮右侧滑出；**全量三层索引**——本地缓存秒开（离线可搜）→ GitHub topic 全部分页一次拉全 → Search API 兜底，**搜索覆盖全部插件**；**每 30 分钟后台自动刷新**并提示新增插件；标签筛选、镜像测速、安装方式选择（npm 预构建优先 / GitHub 源码+镜像）、实时进度、可取消、失败重试、详情二次展开（简介/星标/标签/语言/许可证/跳转链接）
- **安装成功后自动出现「重启服务并重启桌面端」引导横幅**：点一下即自动完成——本地服务重启 + 桌面端整体关闭并重新打开（新实例自动拉起服务进入主界面），插件立即生效，全程无需手动操作
- **命令面板（Ctrl+K）**：搜索所有菜单命令/特效开关/面板入口，回车即执行
- **关闭确认弹窗**（自绘科技风）：最小化到后台 / 直接关闭 / 取消，支持**记住选择**下次不再询问

### 外观与特效
- 点击粒子 / 光标光晕 / 壁纸视差与动态流动（全部可独立开关）
- 可更换背景图（图片 / mp4 / webm / gif，深度可调、持久化）；**恢复默认背景 = 网页版原始背景**
- 内置多套 UI 设计（结构级差异：圆角/玻璃质感/光晕/纹理）：极光（玻璃拟态）/ 赛博（霓虹硬边）/ 翡翠（温润大圆角）/ 午夜（极简金线）
- 可自定义界面图标（标题栏/启动页，即时生效持久化）与 exe 图标（生成多尺寸 ICO，重新打包生效）
- **控制面板**（左上角 🎛 按钮或 Ctrl+K）：七组选项（主题/背景/启动界面/界面/窗口/工具），**点击整行即可切换**
- 深/浅主题自适应；窗口置顶 / 开机自启动；任务完成系统通知（可开关）

### 运行机制
- 系统托盘驻留 + `Ctrl+Alt+D` 全局唤起；单实例、窗口位置/大小记忆
- 关闭桌面版自动停止其拉起的服务（复用外部服务时不影响外部进程）

### 服务拉起与重启流程（已在真实部署机器验证）

- **部署位置固定**：`<安装目录>\deepseekharness-desktop`（目录由安装包选择）。其中：
  - `env\` = Node.js / pnpm / npx 运行时（本机已有则直接复用，不下载）；
  - `workspace\` = 项目依赖，deepseekharness 后端本体在 `workspace\node_modules\@deepseek-ai\dsh\lib\bin.js`。
- **手动拉起（排查用）**：在 `workspace` 目录打开终端，执行 `npx dsh web`（默认端口 3080，几秒即起）。
- **桌面端自动拉起**：同一目录、同一命令（加 `--yes` 与端口参数）。部署与启动共用同一路径公式，部署完成即精准定位 workspace，不需要搜索。失败自动降级：裸 `dsh web` → 绝对路径（8.3 短路径，防目录含空格）。
- **插件安装/卸载后**：点击「重启服务并重启桌面端」→ 内置引擎先强杀 3080 旧服务并等端口释放，再按上述三级方式拉起并校验 `__DSH_BOOT__`（全程无 PowerShell 间接层，进程秒退立即换下一级），成功后桌面端整体重启、直接连上服务。
- **故障排查**：日志都在 `%APPDATA%\deepseekharness-desktop\`：
  - `launch.log` —— **拉起服务全链路日志**（环境快照 → 每次拉起命令/cwd → 端口探测每步结果 → 服务前 10 秒输出 → 最终结论），拉不起时优先看这份；
  - `service-out.log` / `service-err.log` —— 服务自身输出/报错；
  - `server.log` —— 实际启动命令与 cwd；
  - `setup.log` —— 首次部署过程。

## 安装与使用

1. **双击安装包 `desk-harness-setup-1.0.0.exe`**：标准安装向导，可选择安装路径（该路径即部署位置）、创建桌面快捷方式，完成后自动启动。
2. **首次启动自动部署环境**：
   - 自动检测运行环境（Node.js ≥ v22.19.0 / pnpm / npx / 项目依赖 @deepseek-ai/dsh；本机已有 deepseekharness 则直接跳过部署）；
   - 环境缺失时弹出部署窗口：部署位置固定为「**安装目录\deepseekharness-desktop**」（由安装包决定，启动界面不再另行选择），点击「**同意并一键安装配置**」后自动完成配置（Node.js / pnpm / npx 本机已有则直接复用不下载；缺失才自动安装），并安装项目依赖（全程无需手动操作），随后自动拉起服务进入主界面；
   - 部署日志：`%APPDATA%\deepseekharness-desktop\setup.log`（部署窗口内可一键打开）。
3. **日常启动**：非首次启动只检测后台服务是否运行；服务未启动时在 `workspace` 目录内以 `npx dsh web` 自动拉起（与手动启动一致），用户无感知。
4. **卸载**：控制面板「应用和功能」→ DeepSeek Harness 桌面版 → 卸载；或运行安装目录中的 `Uninstall DeepSeek Harness.exe`，一键卸载。卸载会同时删除安装目录下的 `deepseekharness-desktop`（Node/pnpm/依赖）与 `%APPDATA%\deepseekharness-desktop` 配置目录；`C:\Users\<用户>\.dsh`（会话与插件数据）保留。
5. 启动页：首次启动按引导完成 API Key 配置；此后倒计时 3 秒自动进入（或点「立即进入」）。
3. 菜单（标题栏 ☰ 按钮，或 Alt 快捷键）：
   - **文件**：更换背景图（`Ctrl+Shift+B`）/ 恢复默认背景 / 背景图浓度 / **自定义图标（界面 + exe）…** / **恢复默认图标** / 更改服务端口 / 打开配置文件夹 / 关闭时最小化到托盘 / 退出
   - **视图**：重新加载（`Ctrl+R`）/ 全屏（`F11`）/ 缩放 / 开发者工具（`F12`）
   - **特效**：各特效独立开关：
     科技感视觉层 / 点击粒子 / 光标光晕 / 壁纸视差 / 壁纸动态流动 / 自绘标题栏 / Token 胶囊 / 任务进度条
   - **帮助**：关于（含工作区、服务地址、配置目录等诊断信息）
4. 标题栏：拖动移动窗口、双击最大化/还原；右侧为 Windows 原生窗口按钮。
5. **关闭窗口会弹出确认框**：最小化到后台 / 直接关闭程序 / 取消（可记住选择）。
6. 插件商店：
   - 点击标题栏「🧩 dsh-plugin」→ 右侧面板平滑展开（搜索框可筛选，覆盖全部插件）；
   - 单个插件点「安装」；勾选多个后点「批量安装 (n)」；「全选」支持当前筛选结果；
   - **卸载插件**：已安装插件的按钮自动变为红色「卸载」（详情面板同样支持），点击即卸载（pnpm 移除依赖 + 清理激活层），完成后同样出现「重启服务并重启桌面端」引导横幅——一键自动重启后插件完全移除；
   - 安装完成后面板顶部出现「重启服务并重启桌面端」横幅，点击即生效；已安装插件带绿色「已安装」徽标。

## 快捷键

| 快捷键 | 功能 |
| --- | --- |
| `Ctrl+K` | 命令面板（所有命令/开关/面板入口） |
| `` Ctrl+` `` | 终端停靠面板 |
| `Ctrl+Shift+B` | 更换背景图 |
| `Ctrl+R` / `F11` / `F12` | 重载 / 全屏 / 开发者工具 |
| `Ctrl+Alt+D` | 从托盘唤起窗口 |

## 自定义图标

- **自定义图标（界面 + exe 一体）**：控制面板 → 工具 →「自定义图标（界面 + exe）…」（或菜单 文件 → 自定义图标…），选择图片（建议 256×256 以上方形图）后一次生效全部：
  - **界面图标**（标题栏 / 启动页）即时生效并持久化；
  - **窗口 / 任务栏 / 托盘图标**即时生效（任务栏自动刷新缓存，若未立即更新稍候片刻）；
  - 安装包 / exe 文件图标需**重新打包**后生效。
- **恢复默认图标**：控制面板 → 工具 →「恢复默认图标」，界面与 exe 图标一起回到内置默认（任务栏同步刷新）。
- 默认图标源图：`desktop\build\生成EXE图标图片.png`（生成 `build\icon.ico` 与 `build\logo-256.png`）。
- 也可命令行快速设置界面图标：`npx electron . --logo=<图片路径>`。

## 要求与注意

- 安装版无需手动准备任何环境：首次启动自动检测并一键部署（Node.js / pnpm / npx / 依赖安装到「安装目录\deepseekharness-desktop\」，位置由安装包选择，不影响系统其他软件）。
- 应用自管工作区：`<安装目录>\deepseekharness-desktop\workspace`（依赖安装位置；服务在此目录内以 `npx dsh web` 拉起）；也可在 `config.json` 手动指定 `serverBin`/`serverCwd`。
- 后端使用与命令行相同的配置目录（默认 `C:\Users\<用户>\.dsh`），模型/凭据无需重新配置；余额查询直接读取该目录下的 `.credentials.yaml`。
- 自绘标题栏开启时网页内容下移 40px；关闭标题栏后布局完全还原。
- 特效均为纯 CSS/Canvas 叠加层，不修改网页版 DOM；如影响性能可在控制面板逐项关闭。
- 后端日志：`%APPDATA%\deepseekharness-desktop\server.log`。
- 环境安装失败 / 服务启动失败时均有明确提示与「打开日志」入口。

## 从源码构建（GitHub 协作）

```powershell
cd desktop
npm install
npm start                        # 开发模式
npm run pack                     # 打包 NSIS 安装包 -> dist\desk-harness-setup-1.0.0.exe
```

国内镜像加速：
```powershell
$env:ELECTRON_MIRROR = "https://npmmirror.com/mirrors/electron/"
$env:ELECTRON_BUILDER_BINARIES_MIRROR = "https://npmmirror.com/mirrors/electron-builder-binaries/"
```

更换图标：把新图放在仓库根目录 `deepseek.png`，然后：
```powershell
cd build\tools && npm install && node render-icon.js   # 生成 build\icon.ico + logo-256.png
```

详见 `desktop\README.md`。
